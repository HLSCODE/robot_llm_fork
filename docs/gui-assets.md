# GUI 图标与视觉资产清单

本项目当前 GUI 不打包第三方图标集、图片字体或商业视觉素材。

- 工作流画布节点、连线、状态标签和插入按钮由 Qt `QPainter` 运行时绘制。
- 动作库与按钮中的符号使用 Unicode 字符，由操作系统字体渲染；仓库不复制、修改或分发字体文件。
- 中性颜色来自系统 `QPalette`；动作类别色和运行状态色是项目内定义的设计令牌。
- 后续增加 SVG、PNG、字体或第三方图标库时，必须在本文件记录来源、版本、许可证、修改情况和打包路径，再允许进入发布包。

因此当前发布包没有需要单独归属声明的第三方 GUI 视觉资产。
